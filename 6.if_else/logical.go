package main

import "fmt"

func main() {
	var role = "admin"
	var hasPermission = false
	// if role == "admin" || hasPermission {
	// 	// == double equal to use hota h, compare krne k liye
	// 	fmt.Println("yes")
	// || means or

	// }

	if role == "admin" && hasPermission {
		fmt.Println("yes")

		// && means and

	}

}
